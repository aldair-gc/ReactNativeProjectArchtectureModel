import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#0d3db2',
        padding: 10,
        paddingHorizontal: 24,
        borderRadius: 4,
    },
    text: {
        fontSize: 16,
        color: 'white',
        fontWeight: '600',
    },
});
